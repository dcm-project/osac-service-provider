# environment-agent is built from the exact version pinned in the e2e module.
# The workflow places the binary in .bin before building this small runtime image.
FROM registry.access.redhat.com/ubi9/ubi-minimal:latest

WORKDIR /app

COPY .bin/environment-agent .

EXPOSE 8090

ENTRYPOINT ["./environment-agent"]
